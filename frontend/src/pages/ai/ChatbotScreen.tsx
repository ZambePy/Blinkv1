/**
 * REMOVIDO — este arquivo pode ser apagado.
 *
 * A tela de chatbot tinha rota viva, nenhum caminho até ela em nenhuma outra
 * tela, entrada só por `<input>` de teclado físico (inalcançável por olhar) e
 * um POST para um backend que não existe neste produto — cuja mensagem de erro
 * culpava a conexão do usuário.
 *
 * O "chatbot integrado" do roteiro de produto é o ASSISTENTE LOCAL, em
 * `frontend/src/services/assistente/`: sugere palavra no teclado e frase
 * inteira na Conversa, aprende com o próprio paciente e não toca a rede.
 *
 * Apague este arquivo (e a pasta `pages/ai/`) quando puder.
 */
export {};
