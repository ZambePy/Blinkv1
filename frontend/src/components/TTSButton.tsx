/**
 * REMOVIDO — este arquivo pode ser apagado.
 *
 * O `TTSButton` era um `<button>` cru sem altura definida: ~28 px de alvo,
 * contra os ~198 px que uma fixação ocular exige. Só a tela de chatbot o
 * usava, e essa tela saiu (ver o irmão deste arquivo em `pages/ai/`).
 *
 * O conteúdo original está no histórico do git. Apague este arquivo e o
 * `pages/ai/ChatbotScreen.tsx` quando puder; enquanto existirem, são inertes.
 */
export {};
